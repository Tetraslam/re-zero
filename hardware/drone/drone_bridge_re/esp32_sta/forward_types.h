#pragma once

// Arduino generates function prototypes near the top of the sketch; these forward
// declarations keep those prototypes compiling even if the struct definitions
// appear later in the .ino.
struct TcpOut;
struct UdpFlow;

